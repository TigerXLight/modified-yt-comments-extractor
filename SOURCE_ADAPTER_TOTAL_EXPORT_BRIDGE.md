# Source Adapter Total Export Bridge

Builds shared `source_total_export_package` outputs from Adapter Capture Bundle Bridge packages.

This section converts one or more adapter capture bundles into Total Export packages through the shared Total Export contract, writes a package index and Evidence Queue handoff metadata, and keeps the adapter path as executable shared-section capability rather than a planning-only placeholder.
