# Source Adapter Shadow DOM Comment Bridge Runtime

This implementation milestone adds `source_adapter_shadow_dom_comment_bridge_runtime` as a callable runtime surface for the shared Source Adapter pipeline.

It covers shadow-DOM comment bridge rows for host selectors, overlay containers, extraction state, and manual observation reconciliation.

The milestone preserves the `KEYS/ACCOUNTS` label, carries credential references as identifiers/redacted hashes only, records receipt-required rows, and keeps provider/network execution behind explicit operator-approved runtime calls.

Tested status strings:

- `SOURCE_ADAPTER_SHADOW_DOM_COMMENT_BRIDGE_RUNTIME_BUILT`
- `SOURCE_ADAPTER_SHADOW_DOM_COMMENT_BRIDGE_RUNTIME_READY_FOR_NEXT_IMPLEMENTATION_BUNDLE`
- `SOURCE_ADAPTER_SHADOW_DOM_COMMENT_BRIDGE_RUNTIME_ROWS_READY`
