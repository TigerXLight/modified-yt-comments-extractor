# Profile/Media Database V76J Command Index

## Apply/test command

Use the command supplied in ChatGPT for V76J. It applies the pack, compiles the touched modules, runs the panel/review-lane/smoke-readiness/final-handoff tests, prints the smoke checklist, prints the handoff, and ends with git diff checks.

## Confirmation phrases

- WRITE_EXISTING_FOLDER_BATCH_PREVIEW
- MATERIALIZE_PROFILE_MEDIA_DATABASE_SELECTION
- APPLY_PROFILE_MEDIA_REVIEWED_FOLDER_OPERATIONS
- WRITE_RECONCILED_PROFILE_MEDIA_BATCH_PREVIEW

## Recommended manual GUI smoke root

`%TEMP%\ytce_profile_media_manual_gui_smoke`

## Files to send to a future session

Send `main.py`, `profile_media*.py`, `profile_media*_test.py`, `tools/run_profile_media*.py`, `PROFILE_MEDIA_DATABASE_*.md`, and `testdata/profile_media_database*.json`. Exclude `venv`, `dist`, `build`, `.git`, browser profiles, credentials, third-party runtimes, and downloaded media.

### Smoke checkpoint 01

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 02

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 03

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 04

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 05

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 06

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 07

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 08

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 09

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 10

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 11

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 12

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 13

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 14

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 15

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 16

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 17

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 18

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 19

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 20

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 21

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 22

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 23

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 24

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 25

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 26

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 27

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 28

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 29

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 30

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 31

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 32

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 33

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 34

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 35

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 36

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 37

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 38

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 39

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 40

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 41

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 42

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 43

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 44

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 45

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 46

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 47

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 48

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 49

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 50

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 51

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 52

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 53

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 54

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 55

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 56

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 57

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 58

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 59

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 60

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 61

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 62

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 63

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 64

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 65

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 66

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 67

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 68

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 69

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 70

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 71

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 72

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 73

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 74

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 75

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 76

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 77

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 78

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 79

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.

### Smoke checkpoint 80

Confirm the Database workflow still uses explicit inputs only, shows reviewable metadata, preserves source-role/source-chain context, and reports all non-requested operations as false.
