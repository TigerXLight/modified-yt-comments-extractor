# Source Adapter Site Capture Mega Bundle Closeout

This implementation milestone adds `source_adapter_site_capture_mega_bundle_closeout` as a callable runtime surface for the shared Source Adapter pipeline.

It covers site capture mega bundle closeout rows covering authority resolution, capture recipes, archives, evidence, export, GUI, KEYS/ACCOUNTS, and release handoff.

The milestone preserves the `KEYS/ACCOUNTS` label, carries credential references as identifiers/redacted hashes only, records receipt-required rows, and keeps provider/network execution behind explicit operator-approved runtime calls.

Tested status strings:

- `SOURCE_ADAPTER_SITE_CAPTURE_MEGA_BUNDLE_CLOSEOUT_BUILT`
- `SOURCE_ADAPTER_SITE_CAPTURE_MEGA_BUNDLE_CLOSEOUT_READY_FOR_NEXT_IMPLEMENTATION_BUNDLE`
- `SOURCE_ADAPTER_SITE_CAPTURE_MEGA_BUNDLE_CLOSEOUT_ROWS_READY`
