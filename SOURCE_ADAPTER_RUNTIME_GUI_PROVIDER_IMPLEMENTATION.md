# Source Adapter Runtime GUI Provider Implementation

This patch consumes the next-roadmap work-order execution closeout and installs concrete runtime implementation artifacts for GUI/controller routes, provider execution adapters, `KEYS/ACCOUNTS` credential-reference selection, dispatch receipts, and implementation handoff.

The implementation provides deterministic local dispatch surfaces first, while preserving operator-approved execution rows and receipt capture contracts for named live smoke.
